version: "3.9"

services:
  whatsapp-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whatsapp-bot
    ports:
      - "8080:8080"
    environment:
      BOT_ADMIN_TOKEN: ${BOT_ADMIN_TOKEN:-mysecrettoken}
